## Memory Model – Stack vs Heap

This example demonstrates the difference between stack, heap, and global memory
by printing their addresses and observing their lifetime and allocation behavior.

Key concepts:
- Stack allocation and scope
- Heap allocation using malloc/free
- Global memory lifetime
